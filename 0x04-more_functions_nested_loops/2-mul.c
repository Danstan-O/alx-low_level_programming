#include "main.h"

/**
 * mul - multiplying two integers
 * Return: returns the multiplied results
 * @a: the first parameter
 * @b: the second parameter
 */

int mul(int a, int b)
{
int results;
results = a * b;
return (results);
}
